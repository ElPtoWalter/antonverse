# Antonverse v26 · galería ampliada

Versión con nuevas fotos añadidas a la galería.

Cambios:
- Añadidas 18 imágenes nuevas a `assets/images/`.
- Actualizado `script.js` para que aparezcan en la galería de sobres.
- Imágenes optimizadas en `.webp`.
- Mantiene v25:

Para subir:
1. Sube `index.html`, `style.css`, `script.js` y `README.md` a la raíz.
2. Sube las nuevas imágenes de `assets/images/` dentro de `assets/images` en GitHub.

El QR no cambia.
